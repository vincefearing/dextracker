version: "3.8"

services:
  dextracker:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: dextracker
    ports:
      - "3000:3000"
    volumes:
      - .:/app
      - /app/node_modules
